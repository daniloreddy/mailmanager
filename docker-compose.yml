services:
  mailmanager:
    # image: ghcr.io/daniloreddy/mailmanager:main
    build: .
    container_name: mailmanager
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
    # environment:
    #   - MAILMANAGER_API_KEY=your_token_here
    #   - MAILMANAGER_PORT=8080
      # - MAILMANAGER_API_KEY=changeme
      # - MAILMANAGER_PORT=8080