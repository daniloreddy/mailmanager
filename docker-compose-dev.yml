services:
  mailmanager:
    build: .
    container_name: mailmanager-dev
    restart: "no"
    ports:
      - "${PORT:-8080}:${PORT:-8080}"
    env_file:
      - .env
    environment:
      - PYTHONUNBUFFERED=1
    volumes:
      - ./data:/app/data
